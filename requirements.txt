# 🚴 SIMULADOR DE CICLORUTAS - DEPENDENCIAS REQUERIDAS 🚴
# 
# Este archivo lista todas las dependencias necesarias para ejecutar
# el simulador completo de ciclorutas con interfaz gráfica.
#
# Para instalar todas las dependencias, ejecuta:
# pip install -r requirements.txt
#
# Versión mínima de Python: 3.7+

# Simulación de eventos discretos
simpy>=3.0.0

# Computación numérica y matemática
numpy>=1.20.0

# Visualización y gráficos
matplotlib>=3.5.0

# Interfaz gráfica (incluida con Python)
# tkinter - NO requiere instalación separada

# Dependencias adicionales de matplotlib
# (se instalan automáticamente)
# - pillow (para manejo de imágenes)
# - cycler (para estilos de gráficos)
# - kiwisolver (para layout)
# - pyparsing (para parsing)
# - python-dateutil (para fechas)
# - six (para compatibilidad)
